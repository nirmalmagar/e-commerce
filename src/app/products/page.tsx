import ProductCard from "@/components/Products/ProductCard";
import React from "react";

const Products = () => {
  return <ProductCard productName={""} slug={""} imageSrc={""} imageAlt={""} price={""} />;
};

export default Products;
